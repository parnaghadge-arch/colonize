{
  "name": "@colonize/admin-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Society Management Web App \u2014 the console a society's admin, committee and manager use (\u00a778). React + Vite.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@colonize/shared": "*",
    "react": "~19.1.4",
    "react-dom": "~19.1.4",
    "react-router-dom": "^7.18.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.2.0",
    "typescript": "~5.9.2",
    "vite": "^7.3.6"
  }
}
